# LAW-N Bytecode Specification (Draft)

This file defines the initial LAW-N opcodes and their expected behavior.

Examples (future):

- `TIME_WAIT <delta_ms>`
- `NSQL_EXEC <query>`
- `SIGNAL_OPEN <channel>`
- `SIGNAL_CLOSE <channel>`
